@model IEnumerable<TheSocialNetwork.Models.Circle>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CircleName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Users)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CircleName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Users)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.CircleId }) |
                @*@Html.ActionLink("Details", "Details", new { id = item.CircleId }) |*@
                @Html.ActionLink("Delete", "Delete", new { id = item.CircleId })
            </td>
        </tr>
        }
    </tbody>
</table>
